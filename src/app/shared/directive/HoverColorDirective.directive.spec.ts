/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HoverColorDirectiveDirective } from './HoverColorDirective.directive';

describe('Directive: HoverColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
